import React from 'react';
import { render } from '@testing-library/react-native';
import { Text } from 'react-native';

describe('authServices placeholder test', () => {
  it('simple sanity', () => {
    expect(1 + 1).toBe(2);
  });
});